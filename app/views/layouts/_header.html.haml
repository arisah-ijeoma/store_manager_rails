- if user_signed_in?
  = "Hi #{current_user.email}"
  = link_to 'Log Out', destroy_user_session_path, method: :delete
- elsif admin_user_signed_in?
  = "Hi #{current_admin_user.email}"
  = link_to 'Log Out', destroy_admin_user_session_path, method: :delete
